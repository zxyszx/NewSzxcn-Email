# v1.2.80

- 修复新 Updater GHCR 包默认为私有，导致旧安装匿名更新时返回 `unauthorized` 的问题。
- Updater 改为发布到现有公开主镜像包的 `updater-latest` 标签，不要求服务器登录 GHCR。
- 发布流程新增匿名拉取门禁；主镜像或 Updater 镜像不能公开拉取时，不再创建正式 Release。
- 版本检查在 GitHub Releases API 暂时不可用或受限时，会通过受信任的 GitHub Release 跳转地址安全识别最新版本。
- 保留更新前备份、唯一回滚镜像、健康检查成功后清理以及失败自动恢复机制。
